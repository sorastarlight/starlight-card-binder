# V90.3.1 — Binder Details & Full View Repair

No SQL is required.

This update:
- Forces fresh loading of `app.js` and `style.css` so the new Database 2.0 side panel is not hidden by browser cache.
- Makes the Binder details panel compact and scrollable.
- Removes the duplicated Owned badge at the bottom.
- Rebuilds Full View into a desktop two-column layout.
- Keeps mobile Full View vertically scrollable.
- Removes the duplicated description under Card Story.

Replace the deployed `docs` folder and use Ctrl+F5 once.
