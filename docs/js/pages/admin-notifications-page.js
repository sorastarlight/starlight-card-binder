import {adminBroadcastNotification} from '../notification-service.js';import {getMyStaffAccess} from '../staff-service.js';const status=document.getElementById('status'),iconInput=document.getElementById('icon'),titleInput=document.getElementById('title'),bodyInput=document.getElementById('body'),routeInput=document.getElementById('route'),expiresInput=document.getElementById('expires');const access=await getMyStaffAccess();if(!access?.isStaff){document.getElementById('form').hidden=true;status.textContent='Administration access is required.'}document.getElementById('form').addEventListener('submit',async e=>{e.preventDefault();const button=e.submitter;button.disabled=true;status.textContent='Sending…';try{const result=await adminBroadcastNotification({icon:iconInput.value,title:titleInput.value,body:bodyInput.value,route:routeInput.value||null,expiresAt:expiresInput.value?new Date(expiresInput.value).toISOString():null});status.textContent=`Broadcast sent to ${result.recipientCount||0} collectors.`;e.target.reset();iconInput.value='🌟'}catch(error){status.textContent=error.message}finally{button.disabled=false}});

