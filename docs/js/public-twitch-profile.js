import{supabase}from'./supabase-client.js';
const username=new URLSearchParams(location.search).get('username');if(username){const{data}=await supabase.rpc('get_public_twitch_connection_v890',{requested_username:username});if(data?.linked){const box=document.createElement('section');box.className='profile-section';box.innerHTML=`<h2>Connected Account</h2><a class="profile-activity-card" href="https://twitch.tv/${encodeURIComponent(data.login)}" target="_blank" rel="noopener"><strong>📺 ${data.displayName||data.login}</strong><span>@${data.login} on Twitch</span></a>`;document.querySelector('main')?.appendChild(box)}}
