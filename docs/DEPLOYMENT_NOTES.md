# Deployment Notes

This folder has been cleaned of obsolete per-version note files and superseded booster hotfix scripts.

Current booster runtime repair, if needed:

- `supabase/v90_1_5_booster_runtime_repair.sql`

The booster editor runtime initialization fix is included directly in `admin-boosters.html`; no SQL is required for that JavaScript fix.
